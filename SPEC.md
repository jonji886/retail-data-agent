# 零售经营分析 Data Agent

## 1. 文档信息

- 版本：v0.1
- 状态：开发前需求基线
- 项目类型：个人项目 / Data Agent 交付案例
- 目标：通过一个可运行、可演示、可评测的项目，学习并展示数据建模、指标语义层、NLQ、智能报告、异常预警、归因分析和 Agent 质量治理能力。
- 默认语言：中文
- 默认部署：本地优先，后续支持公网部署

## 2. 项目定位

本项目模拟一个连锁零售企业的经营分析 Data Agent。用户可以使用自然语言查询经营数据，获得可追溯的指标结果、图表和分析结论，并通过智能报告、异常预警和归因分析辅助经营决策。

项目不追求成为完整的企业级数据中台，而是聚焦于展示一条完整的交付链路：

```text
业务问题定义 → 数据模型 → 指标语义层 → Agent 调用工具 → 分析结果
       ↑                                                    ↓
       └────────── 评测、Badcase、人工确认与持续调优 ──────────┘
```

## 3. 背景与问题

连锁零售经营人员通常需要回答以下问题：

1. 当前销售、毛利和库存表现如何？
2. 哪些区域、门店、商品或渠道表现异常？
3. 指标变化的主要原因是什么？
4. 是否存在需要立即处理的经营风险？
5. 不同报表中的指标口径是否一致？

传统 BI 工具可以提供固定报表，但业务人员仍需要了解表结构、筛选条件和指标口径。大模型可以降低使用门槛，但如果缺少语义层、权限边界和质量评测，容易出现指标误用、SQL 错误、结论无依据和幻觉问题。

本项目要解决的是：

> 让业务用户用中文提出经营问题，系统基于明确的指标口径和数据来源，生成可解释、可审计、可复用的分析结果。

## 4. 目标用户

### 4.1 业务负责人

- 关注整体经营趋势、区域差异、重点风险和行动建议。
- 不熟悉 SQL，希望直接提问。

### 4.2 区域经理 / 门店经理

- 关注本区域或门店的销售、毛利、客流、库存和排名。
- 需要知道异常发生在哪里、为什么发生、应该优先处理什么。

### 4.3 数据分析师

- 关注指标口径、查询明细、SQL、分析过程和结果可信度。
- 需要检查 Agent 生成的结果并修正 Badcase。

### 4.4 项目交付人员（项目拥有者）

- 负责配置业务场景、指标定义、知识库、评测集和交付说明。
- 通过项目文档模拟售前承诺、交付范围、质量标准和验收结果。

## 5. 项目目标

### 5.1 产品目标

- 支持中文自然语言经营问数。
- 通过语义层统一指标定义和查询逻辑。
- 支持智能经营报告、异常预警和基础归因分析。
- 所有关键回答都能展示数据来源、指标口径和生成 SQL。
- 建立可重复运行的准确率评测和 Badcase 闭环。

### 5.2 求职展示目标

项目完成后，应能证明以下能力：

| 岗位要求 | 项目中的证明材料 |
|---|---|
| SQL 与数据建模 | DuckDB 数据模型、分析 SQL、数据字典 |
| 指标体系与数据治理 | 指标字典、口径规则、数据质量检查 |
| Semantic Layer | 指标 YAML/JSON 定义、维度与时间粒度规则 |
| NLQ / Data Agent | 自然语言到查询计划、工具调用和结果解释 |
| 知识库 / Skill | 指标知识、行业规则、分析 Skill 配置 |
| 智能报告 | 报告模板、示例报告和生成过程 |
| 自动预警 | 预警规则、异常结果、处理建议 |
| 归因分析 | 维度拆解逻辑、归因结果和限制说明 |
| 幻觉治理 | 评测集、Badcase 分类、修复记录、审计信息 |
| 交付能力 | SPEC、架构说明、验收标准、Demo 演示脚本和复盘 |

注意：个人项目不能替代真实客户协同、大型项目交付和关键客户影响经验。项目材料必须真实描述为“个人项目 / 模拟交付案例”。

## 6. 非目标

MVP 阶段暂不实现：

- 真实企业数据接入。
- 复杂多租户权限体系。
- 实时流式计算。
- 自动修改业务系统数据。
- 模型微调。
- 覆盖所有零售业务流程。
- 生产级高可用、容灾和大规模并发。
- 完全自动执行高风险经营决策。

## 7. 业务范围与数据假设

### 7.1 企业设定

虚拟企业“优选生活”是一家拥有多个区域和门店的综合零售企业，同时经营线下门店和线上渠道。

### 7.2 分析范围

- 时间：至少覆盖 12 个月，支持日、周、月粒度。
- 组织：全国、区域、城市、门店。
- 商品：一级类目、二级类目、品牌、SKU。
- 渠道：线下门店、官网、小程序、第三方电商平台。
- 经营指标：销售、订单、客流、毛利、库存、促销。

### 7.3 数据来源

第一阶段使用可重复生成的虚拟数据，支持：

- CSV 作为易读的数据交换格式；
- Parquet 作为可选的分析数据格式；
- DuckDB 作为本地分析数据库。

虚拟数据应包含正常趋势、季节性、促销影响和可解释的异常，以便验证归因分析和预警功能，而不是完全随机生成。

## 8. 核心场景

### 场景 A：自然语言问数

用户输入：“本月华东区域销售额同比下降了多少？”

系统应：

1. 识别时间范围、区域、指标和比较方式；
2. 匹配指标语义定义；
3. 生成结构化查询计划；
4. 生成并执行 SQL；
5. 返回数值、同比变化、图表和简短解释；
6. 展示指标口径、数据范围和 SQL；
7. 对无法确定的口径提出澄清问题。

### 场景 B：经营趋势分析

用户输入：“过去 6 个月整体经营趋势怎么样？”

系统应返回：

- 销售额、订单数、毛利率、客单价趋势；
- 同比或环比变化；
- 关键拐点；
- 可能影响因素；
- 数据不足时明确说明，不强行给出因果结论。

### 场景 C：异常预警

系统定期检查：

- 销售额较历史基线明显下降；
- 毛利率异常下降；
- 库存周转天数过高；
- 缺货率升高；
- 某门店或商品出现连续异常。

预警结果应包括：异常指标、异常对象、异常幅度、检测规则、影响范围、建议下钻方向。

### 场景 D：归因分析

用户输入：“华东区域销售额下降的主要原因是什么？”

系统按预设维度进行贡献度拆解：

1. 区域 → 城市 → 门店；
2. 商品类目 → 品牌 → SKU；
3. 渠道；
4. 订单数、客单价、客流或转化率等驱动指标。

系统必须区分“数据上的贡献关系”和“已验证的业务因果”，默认使用“可能原因”“主要贡献因素”等谨慎表述。

### 场景 E：智能经营报告

用户选择时间范围和组织范围后生成日报或周报，报告包括：

- 核心指标摘要；
- 趋势变化；
- 异常事项；
- 归因结果；
- 重点门店 / 商品；
- 待跟进事项；
- 数据口径和生成时间。

## 9. 指标体系与语义层

### 9.1 指标分层

#### 基础指标

- 销售额：订单实际支付金额，不含取消订单。
- 订单数：支付成功且未取消的订单数量。
- 商品件数：有效订单中的商品销售数量。
- 成本金额：商品成本金额。
- 毛利额：销售额减成本金额。
- 客流量：进入门店或访问线上渠道的用户数。

#### 派生指标

- 毛利率 = 毛利额 / 销售额。
- 客单价 = 销售额 / 订单数。
- 件单价 = 销售额 / 商品件数。
- 库存周转天数 = 平均库存金额 / 期间销售成本 × 期间天数。
- 同比增长率 =（本期值 - 上年同期值）/ 上年同期值。
- 环比增长率 =（本期值 - 上期值）/ 上期值。

### 9.2 语义层最小结构

每个指标至少定义：

```yaml
name: gross_margin_rate
display_name: 毛利率
description: 毛利额占销售额的比例
formula: gross_profit / sales_amount
base_metrics:
  - gross_profit
  - sales_amount
dimensions:
  - region
  - store
  - category
  - channel
time_grains:
  - day
  - week
  - month
filters:
  - cancelled_order = false
format: percentage
owner: 经营分析
```

语义层必须能够约束：

- 指标名称与同义词；
- 指标计算公式；
- 默认过滤条件；
- 支持的维度和时间粒度；
- 可进行的比较方式；
- 指标适用范围；
- 数据来源表和字段；
- 指标口径冲突时的优先规则。

## 10. 知识库与 Skill

### 10.1 知识库内容

- 企业和组织层级说明；
- 指标定义与口径；
- 商品、门店、渠道等业务术语；
- 经营分析常见问题；
- 异常判断规则；
- 报告写作规范；
- 分析结论表达禁用规则。

### 10.2 最小 Skill

- `metric_query`：根据指标、维度和时间生成查询。
- `trend_analysis`：进行同比、环比和趋势判断。
- `anomaly_detection`：根据规则识别异常。
- `contribution_analysis`：按维度进行贡献度拆解。
- `report_generation`：生成经营报告。
- `metric_explanation`：解释指标定义、口径和来源。

Skill 不应只是提示词集合，而应包含明确的输入、输出、适用条件、失败条件和示例。

## 11. Agent 工作流

```text
用户问题
  ↓
意图识别与参数抽取
  ↓
指标 / 维度 / 时间 / 口径校验
  ↓
生成查询计划
  ↓
生成 SQL
  ↓
SQL 安全检查与执行
  ↓
结果校验
  ↓
图表与结论生成
  ↓
展示来源、口径、SQL 和置信提示
```

### 11.1 失败处理

以下情况不得直接编造结果：

- 无法匹配指标；
- 用户问题缺少必要时间范围；
- 请求超出当前数据范围；
- SQL 执行失败；
- 结果为空或样本不足；
- 指标分母为零；
- 用户要求系统不支持的因果结论。

系统应返回可理解的失败原因，并给出改写建议或澄清问题。

## 12. 页面与交互

### 12.1 首页 / 经营总览

- 核心指标卡片；
- 销售和毛利趋势；
- 异常预警列表；
- 重点区域、门店和商品排名；
- 进入对话分析入口。

### 12.2 对话分析页

- 中文问题输入框；
- 推荐问题；
- 结果摘要；
- 图表；
- 明细表；
- SQL 展示；
- 指标口径展示；
- 追问和下钻。

### 12.3 报告页

- 选择报告类型、时间范围和组织范围；
- 生成并查看报告；
- 展示报告引用的数据和规则；
- 支持导出 Markdown 或 HTML。

### 12.4 质量评测页

- 标准问题列表；
- 预期指标、SQL 或结果；
- 实际回答；
- 评测通过 / 失败；
- Badcase 分类；
- 修复前后对比。

## 13. 功能优先级

### P0：MVP 必须完成

- 虚拟零售数据生成；
- DuckDB 建库和基础数据模型；
- 指标字典与语义层配置；
- 自然语言问数；
- SQL 展示和执行结果；
- 基础图表；
- 指标口径说明；
- 10～20 条标准评测问题；
- 基础 Badcase 记录。

### P1：形成完整作品集案例

- 同比 / 环比分析；
- 自动预警；
- 归因分析；
- 智能经营周报；
- 知识库和 Skill 配置；
- SQL 安全检查；
- 结果审计信息；
- 评测报告。

### P2：后续增强

- 多轮上下文分析；
- 用户权限与数据权限模拟；
- 人工确认和反馈流程；
- 公网部署；
- 多数据源接入；
- 报告定时生成；
- 更复杂的预测和库存优化。

## 14. 验收标准

### 14.1 问数验收

- 对至少 20 个标准问题进行测试；
- 至少覆盖单指标、同比、环比、分组排名、趋势和下钻；
- 每个回答展示指标口径和数据时间范围；
- SQL 可执行，结果与人工核验结果一致；
- 无法回答时不生成虚构数字。

### 14.2 质量验收

MVP 目标：

- 标准问题 SQL 执行成功率 ≥ 90%；
- 核心指标识别准确率 ≥ 90%；
- 指标口径正确率 ≥ 90%；
- 对不支持问题的拒答或澄清率 ≥ 90%；
- 每个失败案例都有分类和修复记录。

以上是个人项目的评测目标，不代表生产系统 SLA。

### 14.3 展示验收

应能在 10 分钟内完成一次完整演示：

1. 查看经营总览；
2. 提问一个经营问题；
3. 查看 SQL 和指标口径；
4. 下钻到区域、门店或商品；
5. 查看异常预警；
6. 生成经营周报；
7. 展示一个 Badcase 修复前后对比。

## 15. 技术约束与建议

- 后端：Python。
- 分析数据库：DuckDB。
- 数据格式：CSV / Parquet。
- SQL：所有核心指标尽量通过 SQL 表达，避免把计算逻辑隐藏在 Python 中。
- 前端：优先选择能快速形成可视化 Demo 的 Python Web 框架；后续可替换为独立前端。
- 大模型：通过统一接口封装，避免业务逻辑绑定单一模型。
- 配置：指标、维度、知识和 Skill 尽量配置化。
- 日志：记录用户问题、解析结果、SQL、执行状态、结果摘要和错误信息。
- 安全：只允许执行经过检查的只读 SQL；禁止 INSERT、UPDATE、DELETE、DROP 等操作。

## 16. 推荐模块结构

```text
data_agent/
├── SPEC.md
├── README.md
├── data/
│   ├── raw/
│   ├── generated/
│   └── retail.duckdb
├── configs/
│   ├── metrics/
│   ├── dimensions/
│   └── skills/
├── app/
│   ├── ui/
│   ├── agent/
│   ├── semantic_layer/
│   ├── tools/
│   ├── analytics/
│   └── evaluation/
├── tests/
├── docs/
│   ├── architecture.md
│   ├── data_dictionary.md
│   ├── demo_script.md
│   └── badcases.md
└── scripts/
    ├── generate_data.py
    ├── init_db.py
    └── run_evaluation.py
```

## 17. 项目交付物

- 可运行的本地 Demo；
- 虚拟数据生成脚本；
- DuckDB 初始化脚本；
- 数据字典；
- 指标语义层配置；
- Agent 和工具调用代码；
- 标准问题评测集；
- 评测结果报告；
- Badcase 修复记录；
- 架构说明；
- Demo 演示脚本；
- 项目复盘：已实现能力、未实现能力和后续规划。

## 18. 求职表达原则

简历或面试中建议表述为：

> 独立设计并实现连锁零售经营分析 Data Agent，基于 DuckDB 构建销售、库存和毛利分析数据模型，设计指标语义层与 Skill，支持自然语言问数、异常预警、归因分析和智能经营报告；建立 SQL 正确性、指标口径和 Badcase 评测闭环。

不要表述为：

- 负责某大型零售客户的生产交付；
- 保障真实客户项目准确率达到某数值；
- 主导售前、客户汇报或跨团队项目管理。

除非这些经历确实发生过。

## 19. 主要风险

### 风险一：范围过大

优先完成“问数 + 语义层 + 评测”，再增加报告、预警和归因。

### 风险二：只做 UI，不懂底层

每个演示问题都必须能解释数据表、指标公式、SQL 和结果校验过程。

### 风险三：大模型回答看起来正确但无法验证

所有回答展示数据来源、查询范围和必要的 SQL；建立固定评测集。

### 风险四：虚拟数据缺乏业务逻辑

数据生成脚本应显式植入季节性、促销、门店差异、库存积压和异常事件。

### 风险五：项目变成普通 BI 看板

突出自然语言交互、语义层、Agent 工具编排、质量治理和审计闭环。

## 20. 后续开发顺序

1. 固化业务术语和指标口径；
2. 设计数据模型和虚拟数据生成规则；
3. 创建 DuckDB 数据库并完成基础 SQL；
4. 实现语义层配置和指标查询工具；
5. 实现自然语言问数 MVP；
6. 建立标准评测集和 Badcase 机制；
7. 增加异常预警和归因分析；
8. 增加智能报告；
9. 完成页面、文档和演示脚本；
10. 再考虑公网部署和增强功能。

## 21. 当前默认决策

- 场景：连锁零售，包含线下门店和线上渠道。
- 目标：求职展示与系统学习并重。
- 数据：可重复生成的虚拟数据。
- 数据库：DuckDB。
- 语言：中文。
- 部署：本地优先，后续可部署。
- 核心能力：语义层、NLQ、报告、预警、归因、评测和审计。
- 交付原则：先完成可验证的 MVP，再扩展复杂能力。
