# AGENTS.md

本文件是本项目的 AI Coding Agent 协作规则。目标是让修改范围清晰、结果可验证，并保持项目当前的本地优先 MVP 定位。

## 1. 项目上下文

开始开发前，按任务相关性阅读：

1. `README.md`：运行方式、用户场景和当前能力边界。
2. `SPEC.md`：产品目标、非目标和验收基线。
3. `docs/`：架构、设计决策或工作记录（如果存在）。
4. 相关实现代码、配置和测试。

如果任务涉及 README，额外阅读 `docs/README_GUIDELINES.md`。

不需要为了小范围修改通读整个项目；先定位与需求直接相关的文件。

默认中文交互。

## 2. 工作方式

遵循：

```text
理解 → 计划 → 修改 → 验证 → 交付
```

- 先明确问题、输入、输出和成功标准。
- 简单任务用一句话说明方案即可；复杂任务先拆成可验证步骤。
- 优先复用现有模块和配置，避免无关重构。
- 变更应尽量小、职责清晰、容易回滚。
- 分析问题或修复 Bug 时，先依据代码、配置、日志和测试定位根因，不用临时分支掩盖问题。

## 3. 何时需要确认

以下操作在执行前必须确认，除非用户已经明确授权：

- 修改系统架构、核心数据结构或公开 API。
- 引入新的依赖、外部服务或基础设施。
- 删除、迁移或批量覆盖数据。
- 改变安全边界、权限行为或敏感数据处理方式。
- 需求歧义会导致明显不同的实现结果。

低风险的小范围 Bug 修复、测试补充、格式调整和文档修正可以直接处理；如有假设，在交付时说明。

## 4. 项目特有约束

### Python 与依赖

- 使用现有 Python 代码风格和类型标注；新增函数应明确参数与返回值类型。
- 优先使用现有依赖。新增依赖时说明必要性、替代方案和维护成本。
- 不要为简单功能引入大型框架。
- 不提交 API Key、Secret、Password 或 Token；敏感配置使用环境变量。

### 数据与 SQL

- `configs/metrics/metrics.json` 和语义层是指标口径的单一来源。
- 查询应通过语义层生成，避免在调用方重复实现指标公式。
- `app/tools/sql_runner.py` 只允许只读查询；不得绕过校验执行写操作或任意 SQL。
- 虚拟数据应保留固定种子和可复现处理链路，不覆盖原始数据而不说明原因。
- 分析结论应区分数据贡献关系和已验证的业务因果。

### LLM、Prompt 与评测

- LLM 问数链路只生成结构化查询计划，不直接生成或执行 SQL。
- Prompt 属于代码：应纳入版本控制，修改后说明原因，并运行相关 Golden 评测。
- 修改 NLQ 解析、语义层、查询计划校验或报告生成逻辑时，至少运行单元测试和 Golden 评测。
- 记录来源、口径、生成 SQL 和必要的审计信息；无法确定时明确拒答，不编造结果。
- 关注调用次数、Token 消耗和重复计算，但不要为了节省成本牺牲结果正确性和可审计性。

## 5. 代码质量

- 优先可读性、简单实现和单一职责。
- 不添加未使用的依赖、死代码或隐藏副作用。
- 异常不得静默吞掉；错误信息应包含足够上下文以便定位。
- 命令行脚本可以使用 `print()` 输出用户可见结果；库代码不要用大量临时调试输出。
- 只修改当前需求涉及的文件，不顺手改变无关风格或行为。

## 6. 测试与验证

根据变更范围选择验证强度：

- 解析、语义层、SQL 校验、分析逻辑：补充或运行针对性单测。
- 问数、报告、评测链路：运行主流程或对应脚本。
- 配置、依赖或公共行为变化：运行完整测试和必要的 Smoke Test。

当前常用命令：

```bash
# 单元测试
python3 -m unittest discover -s tests -v

# 语义层与只读查询 Smoke Test
python3 scripts/smoke_query.py

# Golden 评测
python3 scripts/run_evaluation.py

# 本地 Web Demo
streamlit run app/web_app.py
```

至少关注以下场景：

- 主流程和已有 Golden 用例。
- 空输入、非法指标/维度/过滤值、日期边界。
- SQL 写操作、多语句和执行失败。
- 空结果、分母为零、LLM 返回非法计划等异常情况。

如果无法运行某项验证，交付时说明原因和已完成的替代检查。

## 7. 文档与交付

代码完成后检查是否需要同步：

- `README.md`：使用方式、核心能力、配置、架构或能力边界发生变化时更新。
- `SPEC.md`：产品目标、非目标或验收标准发生变化时更新。
- `docs/`：重要架构决策、评测记录或长期工作上下文需要留档时更新。

不要求每个小 Bug 都修改 README、CHANGELOG 或新增设计文档。

交付说明应简要包含：

- 做了什么，以及为什么能解决问题。
- 修改了哪些关键文件。
- 执行了哪些测试或验证，结果如何。
- 已知限制、未验证事项或后续建议。

## 8. 完成标准

任务完成前确认：

- 需求目标已实现，且没有扩大无关改动范围。
- 核心测试或主流程验证通过。
- 异常处理和安全边界没有明显回归。
- 相关文档已同步，或明确说明无需更新。
- 交付结果足以让用户继续运行或复核。
